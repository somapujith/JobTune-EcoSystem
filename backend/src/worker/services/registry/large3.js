'use strict';

/**
 * Service registry entries owned by the "large3" porting slice (ADR-001 Phase 2/3).
 * Add one line per service: { name: ({ db, config, services }) => createXxx({ db, config }) }.
 * Only the "large3" agent edits this file. Aggregated by services/index.js.
 */
module.exports = {};
